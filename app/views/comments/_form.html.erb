<%= form_for([@project, @comment]) do |f| %>
	<div class="field">
		<%= f.label "Name: " %><br />
		<%= f.text_field :commenter %><br />
	</div>
	<div class="field">
		<%= f.label "Comments: " %><br />
		<%= f.text_area :body %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>